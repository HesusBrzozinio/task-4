Utwórz konstroler o zakresie sesyjnym.

W ramach kontrolera utwórz mapê zawieraj¹c¹ nazwê jêzyka jako klucz oraz kod lokali jako wartoœæ

Utwórz metodê nas³uchuj¹c¹ zdarzeñ typu ValueChangeListener (parametr metody), motoda zwraca void.


Z obiektu zdarzenia pobierz wartoœæ jaka zosta³a w nim umieszczona i u¿yj jej jako klucza do pobrania wartoœci. Na podstawie pobranej wartoœci utwórz obiekt klasy Locale.

Pobierz instancjê klasy FacesContext z u¿yciem jej metody statycznej a nastêpnie pobierz z kontekstu UIViewRoot i umieœæ w nim obiekt Locale.

Na stronie header.xhtml dodaj listê rozwijan¹ z mo¿liwymi do wyboru jêzykami (komponent selectOneMenu). Nale¿y powi¹zaæ komponent z metod¹ kontrolera nas³uhuj¹c¹ zdarzeñ. Zmiana wartoœci w liœcie powinna wywo³ywaæ wbudowan¹ javascriptow¹ metodê submit() - sprawdŸ jakimi atrybutami dysponuje komponent listy rozwijanej. 